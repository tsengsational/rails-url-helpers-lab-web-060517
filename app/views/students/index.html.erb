<div>
  <% @students.each do |student| %>
    <div><%=link_to student.to_s, student_path(student) %></div>
    <div><%=link_to student.to_s, activate_student_path(Student.first)%></div>
  <% end %>
</div>

<div>
</div>
